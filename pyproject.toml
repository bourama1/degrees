[project]
name = "degrees"
version = "0.1.0"
description = "Degrees project from course CS50 AI, which finds the shortest path between two actors in a database of actors and movies."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
